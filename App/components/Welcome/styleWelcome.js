import { StyleSheet} from 'react-native';
export const Style = StyleSheet.create({ 
    horizontal: {
        flexDirection: 'row',
        justifyContent: 'space-around',
        padding: 10
      }

})